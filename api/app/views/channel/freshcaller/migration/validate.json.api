if @errors.length > 0
 JSON.generate({"errors": @errors, "agent_limit": @agent_limit}.to_hash)
else
 JSON.generate({"success": "Validated, Can proceed with initiation", 
 				"credits": @account.freshfone_credit.available_credit.to_i, 
 				"agent_limit": @agent_limit 
 				}.to_hash)
end