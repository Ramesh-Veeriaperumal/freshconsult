
# Whenever we change the Structure (add/modify/remove keys),
# we will have to modify the CURRENT_VERSION constant in the controller

@items.map do |item|
  response_hash = {
    id: item.id,
    title: item.title_text,
    active: !item.active.zero?,
    created_at: item.created_at,
    updated_at: item.updated_at
  }
  response_hash[:questions] = item.questions if item.custom_survey?
  response_hash
end.to_json
