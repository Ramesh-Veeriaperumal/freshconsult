json.cache! @agent do
  json.(@agent, :active_since, :available, :created_at, :id, :occasional, :points, :scoreboard_level_id, :signature, :signature_html, :ticket_permission, :updated_at, :user_id, :user)
end
