{
  id: @item.id,
  name: @item.name,
  created_at: @item.created_at.try(:utc),
  updated_at: @item.updated_at.try(:utc),
  canned_responses: @ca_responses.compact.map { |item| item.attributes.slice('id', 'title') }
}.to_json
