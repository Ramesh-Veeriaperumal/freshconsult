sample_hash = if @associated_tickets
                @associated_tickets.map do |item|
                  TicketDecorator.new(item, permissibles: @permissibles).to_associations_hash
                end
              else
                []
              end
sample_hash.to_json
