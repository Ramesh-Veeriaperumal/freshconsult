module AuditLog::Translators::CannedResponseFolder
  def readable_canned_response_folder_changes(model_changes)
    # model_changes.each_key do |attribute|
    #   case attribute
    #     in future if we have to add more attributes under canned response folder then we can translate them here
    # end
    model_changes
  end
end
