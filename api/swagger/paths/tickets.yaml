  /tickets/bulk_delete:
    put:
      tags: [tickets]
      parameters:
        - name: data
          in: body
          required: true
          schema:
            $ref: '../definitions/bulk/input.yaml'
      responses:
        204:
          description: Success
        202:
          description: Accepted
          schema:
            $ref: '../definitions/bulk/response.yaml'