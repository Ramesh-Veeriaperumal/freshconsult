<!DOCTYPE html>
<%= open_html_tag %>
	<head>
    	<meta charset="utf-8">	
		<!-- Always force latest IE rendering engine (even in intranet) & Chrome Frame 
		     Remove this if you use the .htaccess -->
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	  
	  	<title><%= "#{h(current_account.name)} Widget" %></title>
		<%= include_cloudfront_css :widget %>
		<%= include_cloudfront_css :ie_hacks %>		

		<%= include_cloudfront_js :fdwidget %>
		<%= include_cloudfront_js :pattern %>
		<%= include_cloudfront_js :redactor %>
		<!--[if IE]><%= include_cloudfront_js :excanvas %><![endif]-->

	  	<%= yield :head %>
		<script type="text/javascript">
			document.domain = document.domain;
			var $J = jQuery.noConflict();
		</script>
	</head>
    <body>
    	<style> body{ background:transparent; }</style>
    	<div class="feedback-wrapper">
	    	<div class="modal modal-widget modal-panel-active" id="feedback-widget-container">
				<%= yield %>
			</div>
		</div>		
    </body>
</html>
