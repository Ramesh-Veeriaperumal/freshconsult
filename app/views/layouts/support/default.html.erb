<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
	<head>
		<meta http-equiv="content-type" content="text/html; charset=utf-8" />
		<title><%= (yield(:title) ? yield(:title) + ' - ' : '') + Helpdesk::USER_FACING_TITLE %></title>
		
		<link rel="stylesheet" href="/stylesheets/support/support.css" type="text/css" media="screen, projection" />
    <%= javascript_include_tag :defaults  %>

    <%= yield :head %>

  </head>
  <body>

    <div class="header">
      <h1><%= link_to(Helpdesk::USER_FACING_TITLE, support_guides_url) %></h1>
    </div>


    <div class="main">
      <div class="content">
        <%= content_tag(:h2, yield(:title)) if yield(:title) %>
        <%= show_flash %>
        <%= yield  %>
      </div>

      <div class="navigation">

        <%= link_to "Request Support", new_support_ticket_path, :class => :request %>

        <h3>Search User Guides</h3>
        <%= render :partial => '/support/shared/article_search' %> 

        <h3>Jump to User Guide:</h3>
        <ol class="guides">
          <%= Helpdesk::Guide.display_order.visible.all(:conditions => { :account_id => current_account.id }).map { |g| content_tag(:li, link_to(g.name, support_guide_path(g))) } %>
        </ol>

        <%= yield :sidebar %>

      </div>

    </div>

    <div class="footer">
      
    </div>


  </body>
</html>
