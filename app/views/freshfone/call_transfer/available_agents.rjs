page << "jQuery('.ffone_available_agents').removeClass('loading-small sloading');
jQuery('.transfer_call_container').show();
if(freshfonesocket.agentList) { freshfonesocket.agentList.clear();freshfonesocket.agentList = undefined; }"
page << "freshfonesocket.onloadUserarray.push.apply(freshfonesocket.onloadUserarray,#{@freshfone_users.to_json});
 freshfonesocket.removeOfflineAgents(#{@offline_users_id.to_json});
freshfonesocket.populateAvailableAgents();"