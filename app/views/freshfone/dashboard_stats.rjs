page << "freshfonesocket.onloadUserarray = #{@freshfone_users.to_json};freshfonesocket.populateAvailableAgents();"
page << "freshfonesocket.activeCalls = #{@active_calls};freshfonesocket.tryUpdateDashboard();"