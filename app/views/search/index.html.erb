<% content_for :layoutType do -%>single<%- end %>
<div class="big_results">
	<h3 class="title"><%= t('search_results') %></h3>
	<% form_tag '/search', :method => :get do %>
		<input class="search" value="<%= h(@search_key) %>" placeholder="Search" autocomplete="off" name="search_key" />
		<input type="submit" value="<%= t('header.search') %>" class="uiButton blue" />
	<% end %>
	<span class="seperator"></span>
	<!-- h3 class="sub_title">Search for "Freshdesk" has 120 results</h3 -->

	<%= render :partial => 'search_results' %>
	<%= will_paginate @items unless @items.blank?%>
</div>
