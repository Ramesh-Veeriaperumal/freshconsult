<h1>SLA Policy</h1>
<%= link_to 'Add SLA Policy', new_helpdesk_support_plan_path %></td>
<table>
  <tr>
    <th>Name</th>
    
  </tr>

<% @support_plans.each do |plan| %>
  <tr>
    <td><%=h plan.name %></td>
   
    <td><%= link_to 'Show', plan %></td>
    <td><%= link_to 'Edit', edit_helpdesk_support_plan_path(plan) %></td>
    <td><%= link_to 'Destroy', plan, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
<br />

<!--%= link_to 'New customer', new_support_plan_path %-->
</div>
