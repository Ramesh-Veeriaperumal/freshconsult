<%= @note.body %>

<% if @note.attachments && !@note.attachments.empty?%> The following files were attached:

<% @note.attachments.each do |a| %><%= a.content_file_name %>: <%= helpdesk_attachment_url(a, :access_token => @ticket.access_token, :host => @host) %><% end %>
<% end %>
===================

This email is in response to support request #<%= @ticket.id_token %>.

You can visit the following URL to see the status of your request at any time:

<%= support_ticket_url(:id => @ticket.to_param, :access_token => @ticket.access_token, :host => @host) %>
