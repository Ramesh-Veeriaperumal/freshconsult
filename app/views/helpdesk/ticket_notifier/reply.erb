<%= @note.body %>

===================

This email is in response to support request - <%= "'#{@ticket.subject} [#{@ticket.display_id}]'" %>.

You can visit the following URL to see the status of your request at any time:

<%= support_ticket_url(:id => @ticket.to_param, :host => @host) %>
