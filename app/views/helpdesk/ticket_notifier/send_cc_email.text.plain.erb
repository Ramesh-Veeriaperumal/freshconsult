
<%= @ticket.requester.name || @ticket.requester.email %>  submitted a new Ticket to <%= @ticket.account.portal_name || @ticket.account.host%> and requested that we copy you

<%= @body %>


<%=render :partial => 'cloud_files_plaintext_template', :locals => { :cloud_files => @cloud_files} %>
