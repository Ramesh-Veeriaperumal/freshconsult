<% if is_application_installed?(Integrations::Constants::APP_NAMES[:salesforce])  %>   
	<div id="salesforce" class="sidepanel">
		<%
		 @liquid_objects = {"requester" => user}
		 widget_code = get_app_widget_script(Integrations::Constants::APP_NAMES[:salesforce], "salesforce_widget", @liquid_objects) %>
		<%= Liquid::Template.parse(widget_code).render("ticket" => @ticket) %>
</div>
<% end %> 
