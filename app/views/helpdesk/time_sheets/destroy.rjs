unless @new_show_page
	page << "jQuery('#timeentry_#{@time_entry.id}').fadeOut(500, function(){ jQuery(this).remove() });"
	page << "#{pushToIntegratedAppsWithoutLoading(@time_entry, :delete)}"
else
	page << render(:partial => "helpdesk/time_sheets/v2/destroy")
end