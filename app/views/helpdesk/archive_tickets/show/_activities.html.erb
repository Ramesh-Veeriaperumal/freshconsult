<div class="conversation_thread" id="all_activities" rel="activity_container">
	<% unless @activities.blank? %>
		<%= render :partial => "helpdesk/archive_tickets/show/activity", :collection => @activities %>
	<% end %>
</div>

