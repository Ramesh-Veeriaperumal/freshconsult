 	<%= yield :main_panel_title %>
	
 	<%= yield :main_panel_summary %>

	<div class="toolbar" id="toolbar">
	  <%= yield :main_panel_toolbar %>
	</div>  
	
	<%= content_tag('span', yield(:main_panel_selected_toolbar), :id => 'selection-toolbar') if yield(:main_panel_selected_toolbar) %>	

	<%= yield :main_panel_content %>