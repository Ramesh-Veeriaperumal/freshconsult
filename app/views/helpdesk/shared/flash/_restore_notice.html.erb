<% if @items.size == 1 and @items.first.respond_to?("name") %>
	<%= "#{@items.first.name} was restored" %>
<% else %>
	<%= pluralize(@items.length, "#{ @cname } was", "#{ @cname.pluralize } were") %> restored
<% end %>
