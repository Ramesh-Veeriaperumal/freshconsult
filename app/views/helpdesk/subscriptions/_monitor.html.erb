<%= link_to_function(image_tag(spacer_image_url, :width => "24px", :height => "24px"), 				
					"", :class => "monitor" ) if not 
                   (@ticket.subscriptions.map(&:user_id).include? current_user.id) %>

<%= link_to_function(image_tag(spacer_image_url, :width => "24px", :height => "24px"),
 					"", :class => "monitor_active" ) if 
 					(@ticket.subscriptions.map(&:user_id).include? current_user.id) %>