<%= link_to(image_tag(spacer_image_url, :width => "24px", :height => "24px"), 				
					"", :class => "watcher_btn monitor", :href => "#" ) if not 
                   (@ticket.subscriptions.map(&:user_id).include? current_user.id) %>

<%= link_to(image_tag(spacer_image_url, :width => "24px", :height => "24px"),
 					"", :class => "watcher_btn monitor_active", :href => "#" ) if 
 					(@ticket.subscriptions.map(&:user_id).include? current_user.id) %>