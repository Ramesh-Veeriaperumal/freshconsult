<script type="text/javascript">
var tag_list = "<%= escape_javascript(current_account.tags.collect{|tag| tag.name}.join(',')) %>"; 
TICKET_DETAILS_DATA = {
	displayId			: "<%= escape_javascript(h(@ticket.display_id)) %>",
	total_notes			: "<%= @ticket_notes_total %>",
	notes_per_page		: 50,
	notes_pagination_url: "/helpdesk/tickets/<%= escape_javascript(h(@ticket.display_id)) %>/notes?v=2&page=",
	last_note_id		: "<%= @ticket_notes.last.id if @ticket_notes.present? %>",
	fetch_notes_url		: "/helpdesk/tickets/<%= escape_javascript(h(@ticket.display_id)) %>/notes/since", 
	tag_list			: tag_list.split(",")
};
</script>