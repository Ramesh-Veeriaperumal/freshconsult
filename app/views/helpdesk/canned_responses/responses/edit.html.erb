<% content_for :sidebar do -%><%- end %>
<h3 class="title itil-hide"><%=t('.title')%></h3>
<%= form_for(@ca_response, :url => helpdesk_update_canned_responses_folder_path(params[:folder_id], @ca_response.id), :html => {:id => "edit_canned_response", :multipart => true }) do |f| %>
	<%= generate_breadcrumbs(params, f, :cancel_url => helpdesk_show_canned_responses_folder_path(:id => params[:folder_id]), :back_url => helpdesk_show_canned_responses_folder_path(:id => params[:folder_id])) %>
	<%= render :partial => "form", :locals => { :f => f, :ca_response => @ca_response } %>
<% end %>
