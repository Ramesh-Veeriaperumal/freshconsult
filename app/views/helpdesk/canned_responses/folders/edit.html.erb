<% content_for :sidebar do -%><%- end %>
<h3 class="title"><%=t('canned_folders.title')%></h3>
<% form_for(@current_folder, :html => {:id => "edit_canned_response_folder" }) do |f| %>
	<%= render :partial => "folder_form", :locals => { :f => f } %>
	<%= link_to(t('cancel'), "#", :class => "btn", :id => "new_cancel" ) %>
	<%= submit_tag t('save'), :class => "btn btn-primary", :role => "button", :id => "save_here" %>
<% end %>