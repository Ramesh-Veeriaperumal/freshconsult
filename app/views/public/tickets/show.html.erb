<% if current_account.features?(:archive_tickets) and @ticket.is_a?(Helpdesk::ArchiveTicket) %>
  <%= render :partial => "archived_ticket" %>
<% else %>
  <%= render :partial => "ticket" %>
<% end %>
