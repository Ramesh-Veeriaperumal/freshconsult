

<%= simple_format(@config.description) %>

<% if @export_url.present? %>
  You can download the file from <%= @export_url %> (It will expire in 45 days). If the link does not work, copy and paste the URL below in your browser's address bar:
  	 <%=  @export_url %>
<% end %>
