<%= t("mailer_notifier.hi_user", user_name: @user.name) %>

<%= t("mailer_notifier.scheduled_task_notifications.downgraded_users.report_being_sent", scheduled_task_name: @task.schedulable.filter_name) %>
  <%@options[:agent_downgraded].each do |user|%>
  <%= user.email %>
  <%end%>

<%= t("mailer_notifier.scheduled_task_notifications.downgraded_users.update_configuration") %>

<%= "#{@user.account.full_url}/reports/#{HelpdeskReports::Constants::Common::REPORT_ENUM_TO_TYPE[@task.schedulable.report_type]}"%>

<%= t("mailer_notifier.regards") %>
<%= t("mailer_notifier.freshdesk_support_team") %>