# See http://www.robotstxt.org/wc/norobots.html for documentation on how to use the robots.txt file
#
# To ban all spiders from the entire site uncomment the next two lines:
# User-Agent: *
# Disallow: /

User-agent: *
Disallow: /support/search
Disallow: /support/tickets/
Disallow: /support/login/
<% @disallow_languages.each do |lang| %>
Disallow: /<%= lang %>/support/search
Disallow: /<%= lang %>/support/tickets/
Disallow: /<%= lang %>/support/login/
<% end %>
Disallow: /login/normal/
Allow: /helpdesk/attachments
Disallow: /helpdesk/
Disallow: /public/tickets/
Disallow: /*/hit$
<% if @crawl_sitemap %>
Sitemap: <%= @url %>/support/sitemap.xml
<% end %>