New Topic in "<%= @topic.forum.name %>"

<%= "_" * 100 %>
 
<%= @topic.title %>
By: <%= @user.name %> 
	on <%= @topic.created_at.strftime("%a, %b %e, %Y at %l:%M %p") %>

<%= @topic.posts.first.body %>

<%= "_" * 100 %>

View Topic
<%= discussions_topic_url(@topic.id, :host => @user.account.host) %>

Reply
<%= discussions_topic_url(@topic.id, :host => @user.account.host) << "#reply-to-post"%>

View Forum
<%= discussions_forum_url(@topic.forum.id, :host => @user.account.host) %>

Turn off notifications for this forum
<%= toggle_monitorship_url("forum", @topic.forum.id, "unfollow", :host => @user.account.host) %>
 
