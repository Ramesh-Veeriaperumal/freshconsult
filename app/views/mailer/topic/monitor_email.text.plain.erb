New Topic in "<%= @topic.forum.name %>"

<%= "_" * 100 %>
 
<%= @topic.title %>
By: <%= @user.name %> 
	on <%= @topic.user_readable_created_at %>

<%= @topic.posts.first.body %>

<%= "_" * 100 %>

View Topic
<%= discussions_topic_url(@topic.id, :host => @host) %>

Reply
<%= reply_discussions_topic_url(@topic.id, :host => @host, :anchor => 'reply-to-post') %>

View Forum
<%= discussions_forum_url(@topic.forum.id, :host => @host) %>

Turn off notifications for this forum
<%= toggle_monitorship_url("forum", @topic.forum.id, "unfollow", :host => @host) %>
 
