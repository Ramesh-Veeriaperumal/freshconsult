New Topic in "<%= @topic.forum.name %>"

<%= "_" * 100 %>
 
<%= @topic.title %>
By: <%= @user.name %> 
	on <%= formated_date(@topic.created_at,{:include_year => true}) %>

<%= @topic.posts.first.body %>

<%= "_" * 100 %>

View Topic
<%= category_forum_topic_url(@topic.forum.forum_category.id, @topic.forum.id, @topic.id, :host => @user.account.host) %>

Reply
<%= category_forum_topic_url(@topic.forum.forum_category.id, @topic.forum.id, @topic.id, :host => @user.account.host) << "#reply-to-post"%>

View Forum
<%= category_forum_url(@topic.forum.forum_category.id, @topic.forum.id, :host => @user.account.host) %>

Turn off notifications for this forum
<%= toggle_monitorship_url("forum", @topic.forum.id, "unfollow", :host => @user.account.host) %>
 
