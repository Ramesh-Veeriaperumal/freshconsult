<%= t("mailer_notifier.topic_notifications.status_update") %> 
<%= @topic.title %>

<%= "_" * 100 %>

<%= t("mailer_notifier.topic_notifications.current_stamp", forum_type: @forum_type.downcase.singularize, stamp: @current_stamp) %> 

<%= "_" * 100 %>

<%= t("mailer_notifier.topic_notifications.view_topic") %> 
<%= discussions_topic_url(@topic.id, :host => @host) %>

<%= t("mailer_notifier.topic_notifications.reply") %> 
<%= reply_support_discussions_topic_url(@topic.id, :host => @host, :anchor => 'reply-to-post') %>

<%= t("mailer_notifier.topic_notifications.turn_off_topic_notifications") %> 
<%= toggle_monitorship_url("topic", @topic.id, "unfollow", :host => @host) %>
 
