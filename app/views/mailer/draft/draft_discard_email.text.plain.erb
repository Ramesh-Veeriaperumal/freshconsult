<%= t("mailer_notifier.draft_mailer.discard_email.discard_msg_plain", title: @draft[:title], user: @user.name, time: Time.zone.now.strftime("%a, %d %b %Y at %H:%M %p"))%>

<%= "_" * 100 %>

<%= t("mailer_notifier.draft_mailer.discard_email.draft_by_plain", title: @draft[:title], author: @current_author.name)%>

<%= "_" * 100 %>

<%= @draft[:description] %>

<%= "_" * 100 %>

<%= t("mailer_notifier.draft_mailer.discard_email.view_article")%>
<%= solution_article_url(@article, :host => @host) %>
