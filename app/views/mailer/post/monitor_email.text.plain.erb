New Reply on topic "<%= @post.topic.title %>"

<%= "_" * 100 %>
 
<%= @user.name %> said on <%= @post.created_at.strftime("%a, %b %e, %Y at %l:%M %p") %>

<%= @post.body %>

<%= "_" * 100 %>

View Topic
<%= discussions_topic_url(@post.topic.id, :host => @user.account.host) %>

Reply
<%= discussions_topic_url(@post.topic.id, :host => @user.account.host) << "#reply-to-post"%>

Turn off reply notifications for this topic
<%= toggle_monitorship_url("topic", @post.topic.id, "unfollow", :host => @user.account.host) %>