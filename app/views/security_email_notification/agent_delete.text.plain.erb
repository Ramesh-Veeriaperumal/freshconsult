<%= t("mailer_notifier.hi") %>

<%= t("mailer_notifier.security_notification.agent_deleted", agent_name: @model.name, doer: @doer, time: @time) %>
<%if @model.account.subscription.state.downcase.eql?('active')%>
<%= t("mailer_notifier.security_notification.update_agent_billing_plain") %>
<%end%>
<%= t("mailer_notifier.security_notification.deleted_agent_info", agent_name: @model.name) %>

<%= t("mailer_notifier.security_notification.current_agent_stats", full_time: @model.account.all_agents.full_time_agents.size, occasional: @model.account.all_agents.occasional_agents.size) %>

<%= render :partial => "footer_plain", :formats => [:html] %>
