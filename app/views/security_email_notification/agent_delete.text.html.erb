<%= t("mailer_notifier.hi") %><br><br>

<%= t("mailer_notifier.security_notification.agent_deleted", agent_name: @model.name, doer: @doer, time: @time) %>
<%if @model.account.subscription.state.downcase.eql?('active')%>
<%= t("mailer_notifier.security_notification.update_agent_billing", billing_url: "#{@model.account.full_url}/subscription").html_safe %>
<%end%>
<%= t("mailer_notifier.security_notification.deleted_agent_info", agent_name: @model.name) %>
<br><br>
<%= t("mailer_notifier.security_notification.current_agent_stats", full_time: @model.account.all_agents.full_time_agents.size, occasional: @model.account.all_agents.occasional_agents.size) %><br><br>

<%= render :partial => "footer" %>
