<%= render layout: 'shared/email_header_footer.plain.erb', locals: { user_name: @admin.name, sent_by: :freshdesk_support } do %>

<%= t('mailer_notifier.welcome_to_freshdesk') %>
	
<%= t('mailer_notifier.click_to_activate') %> <%= link_to "#{@activation_url}", "#{@activation_url}" %>
	
<% end %>
	

