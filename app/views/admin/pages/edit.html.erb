<% form_for :portal_page, @portal_page, 
    			:url => { :action => :update }, :html => { :method => :put, :target => "_blank" }do |form| %>
	<%= render :partial => 'form', :object => form %>	
<% end %>