<% form_for :portal_page, @portal_page, :url => admin_portal_template_page_path, :html => { :method => :put } do |form| %>
	<%= content_tag :h3, "Customize portal page", :class => "title" %>
  	<%= render :partial => 'form', :object => form %>
<% end %>