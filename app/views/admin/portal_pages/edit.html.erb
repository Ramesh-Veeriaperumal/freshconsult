<% form_for :portal_page, @portal_page, :url => admin_portal_page_path, :html => { :method => :put } do |f| %>
  <%= f.error_messages %>
  <%= f.hidden_field :page_type %>
  <%= f.label @portal_page.page_name %> 
  <%= f.text_area :content, :class => "auto", :rows => 24  %>
<% end %>