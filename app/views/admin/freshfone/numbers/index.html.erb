<%= stylesheet_link_tag_with_rtl :'cdn/freshfone_settings' %>
 
<% content_for :sidebar do %>
	<div class="sidepanel phone_features_info">
		<i class="common-icon-help"></i>
		<h3 class="lead"><%=t('freshfone.admin.numbers.sidebar_title')%></h3>
		<p><%= t('freshfone.admin.numbers.sidebar_content_v2').html_safe %></p>
    		<%= inm_tour_button(t('freshfone.admin.numbers.tour_link'),19418) %>
	</div>
<% end %>

<div class="list-page">
	<div class="list-page-header"	>
		<div class="row-fluid">
			<div class="span6"><h3 class="title"><%= t('freshfone.admin.freshfone_numbers_title') %></h3></div>
			<% unless freshfone_trial_states? && trial_number_limit_exceeded? %>
				<div class="span6 pull-right pagination-right header_buttons align-right" >
					<% if @numbers.any? %>
				 		<%# form_tag(toggle_freshfone_admin_freshfone_path, 
				 					{:class => "toggle_freshfone_form", :method => :put}) do %>
							<%#= check_box_tag :toggle_freshfone, 1, feature?(:freshfone), :class => 'toggle_freshfone' %>
						<%# end %>
						<% if current_account.features?(:freshfone_caller_id_masking) %>
							<a id="verify-outgoing-caller" href="#" class="mr10"><%=t('freshfone.admin.numbers.caller_id.manage_caller_id')%></a>
						<% end %>
						<%= buy_number_link_helper %>
					<% end %>
	    	</div>
	    <% end %>
		</div>
	</div>

	<% if current_account.features?(:freshfone_caller_id_masking) %>
		<%= render :partial => "/freshfone/outgoing_callers_id", :locals => { :settings => false } %>
	<% end %>
	
	<%= render :partial => "/admin/freshfone/numbers/trial_usage"  if freshfone_trial_states? %>
	<%= render :partial => "/admin/freshfone/numbers/purchased_numbers", :locals => {:purchased_numbers => @numbers} %>
</div>

<script type="text/javascript">
	(function($){
		$('.toggle_freshfone').itoggle({
			checkedLabel: "<%= t('enabled') %>",
			uncheckedLabel: "<%= t('disabled') %>"
		}).change(function() {
			$(this).parent().submit();
		});
	})(jQuery);
</script>