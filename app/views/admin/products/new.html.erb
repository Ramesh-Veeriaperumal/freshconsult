<% content_for :sidebar do -%>
  <%= render :partial => "helpnote_without_tour" %>
<%-end %>
<% @page_title = t('.title') %>
<%= content_tag :h3, @page_title, :class => "title" %>
<% form_for :product, @product, :url => admin_products_path, :html => { :multipart => true, :rel => "validate", :method => :post, :id => "ProductForm", "data-rebrand-form" => true, :class => "form-horizontal" }  do |f| %>
  <%= render :partial => "form", :object => f %>
  <div class="button-container">
  	<%= f.submit t('save'), :class => "btn btn-primary pull-right", "data-loading-text" => t('.saving')  %>
  	<%= link_to(t('cancel'), admin_products_path, :class => "btn pull-right", :id => "email-form-cancel" )  %>  	
  </div>
<% end %>
