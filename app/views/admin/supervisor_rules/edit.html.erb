<% content_for :helpcard do %>
  <%= render :partial => "helpnote_without_tour"%>
<% end %>

<h3 class="lead"><%=t('.title')%></h3> 
<%= form_for @va_rule, :url => admin_supervisor_rule_path, :html => { :method => :put , :id => "VirtualAgent", :class => 'supervisor-form' } do |f| %>
	<%= render :partial => "/admin/shared/rule_form", :locals => { :f => f } %>
<% end %>