<% @article_meta.solution_articles.each do |sa| %>
	<%= language_tab(Language.find(sa.language_id), sa.outdated)%>
<% end %>

<%= add_new_lang_article(current_account.applicable_language_ids - @article_meta.solution_articles.map(&:language_id))%>

<%= language_tab(@language) if @article.new_record? %>