  <%= image_tag fav_icon_url(item), :alt => t('favicon'),
                :onerror => "default_image_error(this)",
                :"data-type" => "favicon" %>
  <div class="thumb-file-upload">
    <%= fields_for "#{field_name}[fav_icon_attributes]", item.fav_icon do |fav_icon_form| %>
      <div class="file-upload-wrap">
        <span class="file-upload-text"><%= t("account.change_favicon") %></span>
      </div>
      	<%= fav_icon_form.file_field :content, :class => "upload_size_validity fileAdminUpload portal-fav-icon", :id => "portal-fav-icon" %>
    <% end %>
    <div>
      <% if item.fav_icon.present? %>
        <% default_favicon_url = item.main_portal? ? delete_favicon_account_path : delete_favicon_admin_product_path(item) %>
        <%= link_to(t("account.use_default"), default_favicon_url,
          :confirm => t('fav_delete_msg'), :method => 'delete') %>
      <% end %>
  </div>
    </div>
