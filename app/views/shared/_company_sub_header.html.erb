<div class="row-fluid" id="company_sub_header">
		<% cookies[:contacts_sort] = params[:state] ? params[:state] : cookies[:contacts_sort]  %>
			<% if type.eql?("user") %>
		<div class="span3">
			<div class="dropdown">
                <%=t('contacts.showing')%><a role="button" class="dropdown-toggle drop-right" id="sorting_dropdown" data-toggle="dropdown" href="#"> <b><%= cookies[:contacts_sort].blank? ? t("contacts.status.verified") : t("contacts.status.#{cookies[:contacts_sort]}")  %></b><b class="caret"></b></a>
                <ul class="dropdown-menu" role="menu" aria-labelledby="sorting_dropdown_list" id="sorting_dropdown_list">
                	<li><%= link_to t("contacts.status.verified"), {:state => "verified"}, :title => t("contacts.status_title.verified") %></li>
                	<li><%= link_to t("contacts.status.unverified"), {:state => "unverified"}, :title => t("contacts.status_title.unverified") %></li>
                	<li><%= link_to t("contacts.status.all"), {:state => "all"}, :title => t("contacts.status_title.all") %></li>
                	<li><%= link_to t("contacts.status.deleted"), {:state => "deleted"}, :title => t("contacts.status_title.deleted") %></li>
                	<li><%= link_to t("contacts.status.blocked"), {:state => "blocked"}, :title => t("contacts.status_title.deleted") %></li>
                </ul>
			</div> 
	    </div>
	<% end %>
	<div class="contacts_sub_header span9 pull-right" align="right">
		<ul class="alpha_sort_list">		
			<%= hidden_field_tag :letter, '' %>
			<li class="first <%= 'active' if params[:letter].blank? %>">
				<%= link_to 'All', :letter => '', :state => params[:state].blank? ? 'verified' : params[:state] %>
			</li>
			<% "A".upto("Y") do |l| -%>	    
			 <li class="<%= 'active' if params[:letter] == [l] %>">
			 	<%= link_to l, :letter => l, :state => params[:state].blank? ? 'verified' : params[:state] %>
			 </li>	
			<% end %>
			<li class="last <%= 'active' if params[:letter] == ['Z'] %>">
				<%= link_to 'Z', :letter => 'Z', :state => params[:state].blank? ? 'verified' : params[:state] %>
			</li>
		</ul>
	</div>
</div>
