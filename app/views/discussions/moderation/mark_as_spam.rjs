page << "jQuery('##{dom_id(@post)} .ficon-notice-o').removeTip();"
page << "jQuery('##{dom_id(@post)}').replaceWith('#{escape_javascript( render :partial => 'discussions/topics/post', :locals => { :post => @post })}')"
page << "App.Discussions.setSpamCounts(#{@counts.to_json.html_safe});"
