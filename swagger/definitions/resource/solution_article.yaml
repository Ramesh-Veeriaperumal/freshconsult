type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      type:
        type: integer
        format: int64
      category_id:
        type: integer
        format: int64
      folder_id:
        type: integer
        format: int64
      folder_visibility:
        type: integer
        format: int64
      thumbs_up:
        type: integer
        format: int64
      thumbs_down:
        type: integer
        format: int64
      hits:
        type: integer
        format: int64
      seo_data:
        type: object
        properties:
          meta_title:
            type: string
          meta_description:
            type: string
          meta_keywords:
            type: string
      agent_id:
        type: integer
        format: int64
      language_id:
        type: integer
        format: int64
      title:
        type: string
      description:
        type: string
      description_text:
        type: string
      status:
        type: integer
        format: int64
      path:
        type: string
      visibility:
        type: object
      attachments:
        type: array
        items:
          $ref: '#/definitions/attachment'
      cloud_files:
        type: array
        items:
          $ref: '#/definitions/extended_cloud_file'
      modified_at:
        type: string
        format: date-time
      modified_by:
        type: integer
        format: int64