  type: object
  description: freshfone call
  properties:
    id:
      type: integer
      format: int64
    duration:
      type: integer
      format: int64      
      description: duration is in seconds
    recording:
      $ref: '#/definitions/attachment'
