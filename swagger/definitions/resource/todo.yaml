type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      body:
        type: string
      completed:
        type: boolean
      ticket_id:
        type: integer
        format: int64
      ticket_subject:
        type: string
      user_id:
        type: integer
        format: int64
      company_id:
        type: integer
        format: int64
      company_name:
        type: string
      contact_id:
        type: integer
        format: int64
      contact_name:
        type: string
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
      reminder_at:
        type: string
        format: date-time 