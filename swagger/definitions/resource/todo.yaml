type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      body:
        type: string
      completed:
        type: boolean
      ticket_id:
        type: integer
        format: int64
      user_id:
        type: integer
        format: int64
