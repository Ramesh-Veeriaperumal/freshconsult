type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      name:
        type: string
      host:
        type: string
      default:
        type: boolean
      product_id:
        type: number
      ssl_enabled:
        type: boolean
      solution_category_ids:
        type: array
        items:
          type: number
      discussion_category_ids:
        type: array
        items:
          type: number