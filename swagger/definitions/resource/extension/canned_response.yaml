type: object
allOf:
  - $ref: '#/definitions/canned_response'
  - properties:
      folder_id:
        type: integer
      evaluated_response:
        type: string
        description: Has to be sideloaded with ?include=evaluated_response
