type: object
allOf:
  - $ref: '#/definitions/canned_response'
  - properties:
      content:
        type: string
      content_html:
        type: string
      attachments:
        type: array
        items:
          type: string
          format: byte
