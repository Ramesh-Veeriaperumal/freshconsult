type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      cc_emails:
        type: array
        items:
          type: string
      fwd_emails:
        type: array
        items:
          type: string
      reply_cc_emails:
        type: array
        items:
          type: string
      fr_escalated:
        type: boolean
      spam:
        type: boolean
      to_emails:
        type: array
        items:
          type: string
      is_escalated:
        type: boolean
      association_type:
        type: integer
        enum: [1,2,3,4]
      associated_tickets_count:
        type: integer
      description_text:
        type: string
      cloud_files:
        type: array
        items:
          $ref: '#/definitions/extended_cloud_file'
