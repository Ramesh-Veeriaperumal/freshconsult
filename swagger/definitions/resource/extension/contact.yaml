
type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      active:
        type: boolean
      deleted:
        type: boolean
      whitelisted:
        type: boolean
      deleted_agent:
        type: boolean
      avatar:
        $ref: '#/definitions/avatar'
      csat_rating:
        type: integer
      preferred_source:
        type: string
