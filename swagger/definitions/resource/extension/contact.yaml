
type: object
allOf:
  - $ref: '#/definitions/extension_generic'
  - properties:
      active:
        type: boolean
      deleted:
        type: boolean
      whitelisted:
        type: boolean
      avatar:
        $ref: '#/definitions/avatar'
