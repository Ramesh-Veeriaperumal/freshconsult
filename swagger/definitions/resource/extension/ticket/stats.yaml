type: object
properties:
  stats:
    type: object
    description: Has to be sideloaded with ?include=stats
    properties:
      agent_responded_at:
        type: string
        format: date-time
      requester_responded_at:
        type: string
        format: date-time
      resolved_at:
        type: string
        format: date-time
      first_responded_at:
        type: string
        format: date-time
      closed_at:
        type: string
        format: date-time
      status_updated_at:
        type: string
        format: date-time
