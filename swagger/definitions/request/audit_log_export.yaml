type: object
properties:
  from:
    type: string
    enum: ["2018-11-05"]
  to:
    type: string
    enum: ["2018-11-30"]
  recieve_via:
    type: string
    enum: ['email/api']
  condition:
    type: string
  filter:
    type: object
    properties:
      filter_set_1:
        type: object
        properties: 
          entity: 
            type: array
            items:
              type: string
          ids:
            type: array
            items:
              type: integer
      performed_by:
        type: array
        items:
          type: integer
      action:
        type: array
        items:
          type: string
required:
  - from
  - to
  - recieve_via
