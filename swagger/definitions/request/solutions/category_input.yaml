type: object
properties:
  name:
    type: string
  description:
    type: string
  visible_in_portals:
    type: array
    description: Array of portal ids to be mapped to category. Allowed only if the account is configured with multiple portals.
    items:
      type: integer