type: object
properties:
  fields:
    type: object
    properties:
      default_fields:
        type: array
        items: 
          type : string
          enum: ["name", "description", "domains", "notes", "health_score", "account_tier", "industry", "renewal_date"] 
          uniqueItems: true
      custom_fields:
        type: array
        items:
          type: string
          uniqueItems: true
