type: object
allOf:
  - $ref: '#/definitions/note'
  - properties:
      user_id:
        type: number
      attachments:
        type: array
        items:
          type: string
          format: byte
      attachment_ids:
        type: array
        items:
          type: number
      from_email:
        type: string
      cc_emails:
        type: array
        items:
          type: string
      bcc_emails:
        type: array
        items:
          type: string
  