type: object
allOf:
  - $ref: '#/definitions/note'
  - properties:
      full_text:
        type: string
      user_id:
        type: integer
      attachments:
        type: array
        items:
          type: string
          format: byte
      attachment_ids:
        type: array
        items:
          type: integer
      from_email:
        type: string
      cc_emails:
        type: array
        items:
          type: string
      bcc_emails:
        type: array
        items:
          type: string
      send_survey:
        type: boolean
      last_note_id:
        type: number
      inline_attachment_ids:
        type: array
        items:
          type: integer
      ticket:
        $ref: ./definitions/request/tickets/update_properties.yaml
        