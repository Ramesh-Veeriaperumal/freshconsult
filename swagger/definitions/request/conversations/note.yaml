type: object
allOf:
  - $ref: '#/definitions/note'
  - properties:
      user_id:
        type: integer
      attachments:
        type: array
        items:
          type: string
          format: byte
      attachment_ids:
        type: array
        items:
          type: integer
      incoming:
        type: boolean
      notify_emails:
        type: array
        items:
          type: string
      private:
        type: boolean