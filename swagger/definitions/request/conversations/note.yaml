type: object
allOf:
  - $ref: '#/definitions/note'
  - properties:
      user_id:
        type: id
      attachments:
        type: array
        items:
          type: string
          format: byte
      attachment_ids:
        type: array
        items:
          type: id
      incoming:
        type: boolean
      notify_emails:
        type: array
        items:
          type: string
      private:
        type: boolean