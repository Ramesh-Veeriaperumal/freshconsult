type: object
allOf:
  - $ref: '#/definitions/note'
  - properties:
      full_text:
        type: string
      include_quoted_text:
        type: boolean
      include_original_attachments:
        type: boolean
      agent_id:
        type: integer
      attachments:
        type: array
        items:
          type: string
          format: byte
      attachment_ids:
        type: array
        items:
          type: integer
      cloud_file_ids:
        type: array
        items:
          type: integer
      from_email:
        type: string
      to_emails:
        type: array
        items:
          type: string
      cc_emails:
        type: array
        items:
          type: string
      bcc_emails:
        type: array
        items:
          type: string
      inline_attachment_ids:
        type: array
        items:
          type: integer
  