type: object
required:
  - category_ids
properties:
  category_ids:
    type: array
    description: Array of category ids to be mapped to the bot
    items:
      type: integer
      uniqueItems: true
