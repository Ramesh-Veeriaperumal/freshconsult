type: object
properties:
    contact:
        type: object
        allOf:
          - $ref: '#/definitions/contact'
          - properties:
              view_all_tickets:
                type: boolean
    company:
        type: object
        allOf:
          - $ref: '#/definitions/company'