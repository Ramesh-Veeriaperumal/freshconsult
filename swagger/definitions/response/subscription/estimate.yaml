type: object
properties:
  immediate_invoice:
    type: object
    properties:
      date_from:
        type: string
        format: date-time
      date_to:
        type: string
        format: date-time
      amount:
        type: integer
      amount_due:
        type: integer
      credits_applied:
        type: integer
      discount_amount:
        type: integer
      days_remaining:
        type: integer
  next_invoice:
    type: object
    properties:
      date_from:
        type: string
        format: date-time
      date_to:
        type: string
        format: date-time
      amount:
        type: integer
      discount_amount:
        type: number
        format: float
  credit_amount_available:
    type: integer
  agent_seats:
    type: integer
  renewal_period:
    type: integer
  plan_id:
    type: integer
    format: int64
  plan_cost:
    description: 'Plan cost per agent per month'
    type: integer
  tax_inclusive:
    type: boolean
