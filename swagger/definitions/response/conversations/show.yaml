type: object
allOf:
  - $ref: '#/definitions/note_create'
  - properties:
      source:
        type: number
      to_emails:
        type: array
        items:
          type: string
      from_email:
        type: string
      cc_emails:
        type: array
        items:
          type: string
      bcc_emails:
        type: array
        items:
          type: string
      freshfone_call:
        $ref: '#/definitions/freshfone_call'
