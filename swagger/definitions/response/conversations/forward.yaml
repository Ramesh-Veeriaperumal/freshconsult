type: object
allOf:
  - $ref: '#/definitions/note'
  - $ref: '#/definitions/extended_note'
  - properties:
      agent_id:
        type: id
      from_email:
        type: string
      forwarded_to:
        type: array
        items:
          type: string
      cc_emails:
        type: array
        items:
          type: string
      bcc_emails:
        type: array
        items:
          type: string
      attachments:
        type: array
        items:
          $ref: '#/definitions/attachment'
