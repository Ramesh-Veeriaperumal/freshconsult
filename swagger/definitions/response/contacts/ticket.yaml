type: array
items:
  type: object
  properties:
    description_text:
      description: Ticket description
      type: string
    tags:
      description: Ticket tags
      type: array
      items:
        type: string
    responder_id:
      description: Ticket's responder id
      type: integer
    due_by:
      description: Ticket's due by time
      type: string
      format: date-time
    requester_id:
      description: Ticket's requester id
      type: string
    subject:
      description: Ticket subject
      type: string
    group_id:
      description: Ticket's group id
      type: string
    status:
      description: Ticket's status
      type: string
    fr_due_by:
      description: Ticket's first response due by
      type: string
    stats:
      $ref: '#/definitions/ticket_stats'
    priority:
      description: Ticket's priority
      type: integer
    created_at:
      description: Ticket's created time
      type: string
      format: date-time