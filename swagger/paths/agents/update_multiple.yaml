patch:
  tags: [agent]
  summary: Update multiple agents
  description: Update multiple agents
  operationId: updateMultipleAgents
  consumes:
  - application/json
  produces:
  - application/json
  parameters:
    - name: agents
      in: body
      required: true
      schema:
        $ref: '#/definitions/update_multiple_agents_input'
  responses:
    202:
      description: Accepted
      schema:
        $ref: '#/definitions/bulk_response'
    404:
      description: Resource not found
    403:
      description: Access denied
