post:
  tags: [agent]
  summary: Gdpr Acceptance
  responses:
    204:
      description: Success
    403:
      description: Access denied
      schema:
        $ref: '#/definitions/bad_request_error'