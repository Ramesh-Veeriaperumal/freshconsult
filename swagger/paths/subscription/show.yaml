get:
  tags: [subscription]
  description: Account subscription details
  summary: Account subscription details
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/subscription_show'
    403:
      description: Access denied
