get:
  tags: [ticket]
  summary: Draft **
  description: Agent specific draft version of the reply for a ticket.
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/ticket_save_draft'
    404:
      description: Ticket not found
    403:
      description: Access denied