get:
  tags: [ticket]
  summary: Activities
  parameters:
    - $ref: '#/parameters/id'
    - name: limit
      in: query
      type: integer
      format: int64
    - name: before_id
      in: query
      type: integer
      format: int64
    - name: since_id
      in: query
      type: integer
      format: int64
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/ticket_activity'
    404:
      description: Ticket not found
    403:
      description: Access denied