get:
  tags: [ticket]
  summary: Activities
  parameters:
    - name: limit
      in: path
      type: number
    - name: before_id
      in: path
      type: number
    - name: since_id
      in: path
      type: number
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/ticket_activity'
    404:
      description: Ticket not found
    403:
      description: Access denied