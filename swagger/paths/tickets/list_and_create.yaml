get:
  tags: [ticket]
  description: Listing all tickets with pagination & applied filter
  summary: List all
  parameters:
    - name: query_hash
      in: query
      type: string
    - name: filter
      in: query
      type: string
      enum: ["spam", "deleted", "overdue", "pending", "open", "due_today", "new", "new_and_my_open", "all_tickets", "unresolved", "article_feedback", "my_article_feedback", "watching", "on_hold", "raised_by_me"] 
    - name: include
      in: query
      type: string
      description: Comma-separated values
      enum: ["requester", "survey", "company", "count"]
    - name: ids
      in: query
      type: string
      description: Comma-separated values
    - name: company_id
      in: query
      type: integer
    - name: requester_id
      in: query
      type: integer
    - name: email
      in: query
      type: string
    - name: updated_since
      in: query
      type: string
      format: date-time
    - name: order_by
      in: query
      type: string
      enum: ["due_by", "created_at", "updated_at", "priority", "status", "agent_responded_at", "requester_responded_at"]
    - name: order_type
      in: query
      type: string
      enum: ["asc", "desc"]
    - name: article_id
      in: query
      type: integer
      format: int64
      description: Article ID for which feedbacks have to be displayed
    - name: language_id
      in: query
      type: integer
      format: int64
      description: Language for which solution article feedbacks have to be retrieved

  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/ticket_index'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'

post:
  tags: [ticket]
  description: Create a ticket
  parameters:
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/ticket_input'
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/ticket_show'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'