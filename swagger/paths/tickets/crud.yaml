
  get:
    tags: [ticket]
    summary: Show Ticket
    parameters:
      - name: id
        in: path
        type: number
        description: Display ID of ticket
        required: true
      - name: include
        in: query
        type: string
        description: Comma-separated values
        enum: ["conversations", "requester", "company", "stats", "survey"]
    responses:
      200:
        description: Success
        schema:
          $ref: '#/definitions/ticket_show'
      404:
        description: Ticket not found
      403:
        description: Access denied
  
  delete:
    tags: [ticket]
    summary: Delete Ticket
    parameters:
      - name: id
        in: path
        type: number
        description: Display ID of ticket
        required: true
    responses:
      204:
        description: Success
      404:
        description: Ticket not found
      403:
        description: Access denied

