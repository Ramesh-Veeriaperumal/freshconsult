put:
  tags: [ticket]
  summary: Save Draft **
  description: Agent specific draft version of the reply for a ticket will be saved.
  parameters:
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/ticket_save_draft'
  responses:
    204:
      description: Success
    404:
      description: Ticket not found
    403:
      description: Access denied