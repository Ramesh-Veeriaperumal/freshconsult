put:
  tags: [Solution Article]
  summary: Update articles suggested
  parameters:
    - name: articles_used
      in: body
      required: true
      schema:
        $ref: '#/definitions/articles_suggested'
  responses:
    204:
      description: Success
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied
