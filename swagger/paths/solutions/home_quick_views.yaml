get:
  tags: [solutions]
  summary: Quick Views
  parameters:
    - name: portal_id
      in: query
      type: string
      description: Portal ID
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/solution_home_quick_view_response'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied
