get:
  tags: [Solution Folder]
  summary: Show folder
  parameters:
    - name: id
      in: path
      type: integer
      format: int64
      required: true
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/folder_response'
    403:
      description: Access denied

put:
  tags: [Solution Folder]
  summary: Update folder
  parameters:
    - $ref: '#/parameters/id'
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/folder_input'
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/folder_response'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied

delete:
  tags: [Solution Folder]
  summary: Delete folder
  parameters:
    - name: id
      in: path
      type: integer
      format: int64
      required: true
  responses:
    204:
      description: Success