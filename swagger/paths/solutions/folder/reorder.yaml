put:
  tags: [Solution Folder]
  summary: Reorder a folder
  parameters:
    - $ref: '#/parameters/id'
    - name: position
      in: body
      required: true
      schema:
        $ref: '#/definitions/solutions_reorder'
  responses:
    204:
      description: No content
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied