get:
  tags: [solutions]
  summary: Solutions summary
  parameters:
    - name: portal_id
      in: query
      type: string
      description: Portal ID
      required: true
    - name: language
      in: query
      type: string
      description: Language Code
      required: false
  responses: 
    200: 
      description: Success
      schema:
        $ref: '#/definitions/solution_home_summary_response'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied    
