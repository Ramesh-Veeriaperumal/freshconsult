get:
  tags: [proactive rules]
  summary: List all
  responses:
    200:
      description: Success
      schema:
        type: object
        properties:
          rules:
            type: array
            items:
              $ref: '#/definitions/proactive_rule_response'
          meta:
            type: object
            allOf:
              - $ref: '#/definitions/meta'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'

post:
  tags: [proactive rules]
  description: Create rule
  parameters:
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/proactive_rule_input'
  responses:
    201:
      description: Success
      schema:
        $ref: '#/definitions/proactive_rule_response'
    404:
      description: rule not found
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
