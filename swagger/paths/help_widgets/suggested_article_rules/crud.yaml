delete:
  summary: Suggested article rules delete
  tags: [Help Widget]
  parameters:
    - name: id
      type: integer
      format: int64
      in: path
      required: true
    - name: rule_id
      type: integer
      format: int64
      in: path
      required: true
  responses:
    204:
      description: Success
    404:
      description: Suggested article rule not found
    403:
      description: Access denied
