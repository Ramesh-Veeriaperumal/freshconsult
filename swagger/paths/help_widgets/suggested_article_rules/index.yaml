get:
  tags: [Help Widget]
  description: Listing all widgets associated to the account
  summary: Suggested article rules List
  parameters:
    - name: id
      in: path
      type: integer
      format: int64
      required: true
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/help_widget_suggested_article_rules_list_response'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied
