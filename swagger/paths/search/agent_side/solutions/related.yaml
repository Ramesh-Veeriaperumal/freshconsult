get:
  summary: Ticket Related Solutions
  tags: [search]
  parameters:
    - name: id
      in: path
      type: number
      description: Ticket ID ** Currently not working
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/search_ticket_solutions'
    403:
      description: Access denied