get:
  tags: [email_notifications]
  summary: Show
  parameters:
    - name: id
      in: path
      type: string
      description: Identifier of the email notification
      required: true
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/email_notification_show'
    400:
      description: Bad request error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied

put:
  tags: [email_notifications]
  summary: Update
  description: Update Bot
  parameters:
    - name: id
      in: path
      type: string
      description: Identifier of the email notification
      required: true
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/email_notification_update'
  responses:
    204:
      description: Success
    400:
      description: Bad request error
    403:
      description: Access denied
