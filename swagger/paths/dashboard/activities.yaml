get:
  tags: [dashboard_activities]
  summary: Dashboard activities
  parameters:
    - name: since_id
      in: query
      type: number
    - name: before_id
      in: query
      type: number
    - name: page
      in: query
      type: number
    - name: per_page
      in: query
      type: number
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/dashboard_activities'
    404:
      description: Activities not found
    403:
      description: Access denied