put:
  summary: Restore Contact
  tags: [contact]
  parameters:
    - name: id
      type: integer
      format: int64
      in: path
      required: true
  responses:
    204:
      description: Success
    404:
      description: User not found