post:
  tags: ['email mailboxes']
  summary: Verify mailbox
  parameters:
    - $ref: '#/parameters/id'
  responses:
    204:
      description: Success
    404:
      description: Mailbox not found
    409:
      description: If the mailbox is in a state that doesn't require/support verification
      schema:
        $ref: '#/definitions/bad_request_error'
