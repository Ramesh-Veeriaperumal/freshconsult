get: 
  tags: [Account Languages]
  summary: Get languages
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/languages'
put:
  tags: [Account Languages]
  summary: Update languages
  parameters: 
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/languages'
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/languages'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'