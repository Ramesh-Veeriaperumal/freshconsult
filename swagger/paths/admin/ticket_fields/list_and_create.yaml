post:
  tags: [ticket fields]
  description: Create a ticket field
  parameters:
    - name: data
      in: body
      required: true
      schema:
        $ref: '#/definitions/ticket_field_request'
  responses:
    201:
      description: Success
      schema:
        $ref: '#/definitions/ticket_field_response'
    400:
      description: Bad Request Error
      schema:
        $ref: '#/definitions/bad_request_error'