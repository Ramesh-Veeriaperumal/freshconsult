get:
  tags: [FreddySkill]
  summary: List of eligible freddy skills
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/freddy_skills_show_and_update'
    403:
      description: Access denied
