get:
  tags: [Scoreboard level]
  summary: List of scoreboard levels
  responses:
    200:
      description: Success
      schema:
        type: object
        properties:
          agent_levels:
            type: array
            items:
              $ref: '#/definitions/scoreboard_level'
    403:
      description: Access denied
