get:
  tags: [Topic]
  summary: Topic's first post
  parameters:
    - name: id
      in: path
      type: string
      description: Identifier of the topic
      required: true
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/topic_first_post'
    400:
      description: Bad request error
      schema:
        $ref: '#/definitions/bad_request_error'
    403:
      description: Access denied
    404:
      description: Resource not found
