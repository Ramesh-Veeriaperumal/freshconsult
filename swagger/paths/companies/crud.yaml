get:
  tags: [company]
  summary: View a company
  parameters:
    - name: id
      in: path
      type: number
      required: true
  responses:
    200:
      description: Success
      schema:
        $ref: '#/definitions/extended_company'