# Used in account class to fill the pod info as part of shard_mapping
CURRENT_POD: poduseast # values can be poduseast, podeu. This should exactly match with the values of Nginx. Else routing may fail.
