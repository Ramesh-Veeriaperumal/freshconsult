# Shoryuken settings
# Ref: https://github.com/phstc/shoryuken/wiki/Shoryuken-options
:concurrency: <%= @concurrency %>
:logfile: <%= @logfile%>
:pidfile: <%= @pidfile %>
:timeout: 30
:delay: 0.1
:queues:
  <% @queues.to_a.each do |q| %>
    - [<%= q.join(', ') %>]
  <% end %>
